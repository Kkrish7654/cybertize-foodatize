import React from 'react'

const AddButton = ({title}) => {
  return (
    <button className={`w-full h-[50px] text-[#666666] text-[16px] bg-white rounded-lg`}>{title}</button>

  )
}

export default AddButton